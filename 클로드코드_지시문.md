# 클로드 코드에 붙여넣을 지시문 (Railway 마이그레이션)

아래 내용을 그대로 복사해서 VS Code의 클로드 코드 채팅에 붙여넣으세요.
중간에 막히면 그 단계를 캡처해서 (claude.ai 채팅의) 저에게 물어보세요.

---

## 클로드 코드에게 (이 아래부터 복사)

안녕. 지금 `dashboard.html`이라는 완성된 사내 대시보드가 있어. 지금은 Firebase(로그인 + Firestore 저장)를 쓰고 있는데, 이걸 전부 걷어내고 **GitHub + Railway**로 옮기려고 해. 나는 비개발자라서, 터미널 명령은 한 줄씩 복사해서 붙일 수 있게 알려주고, 개념은 비유로 설명해줘. 한 단계씩, 내가 "다음"이라고 하면 진행해줘.

### 목표 구조
- 프론트엔드: 지금 `dashboard.html`을 최대한 그대로 사용 (화면·차트·계산 로직 유지)
- 백엔드: Node.js + Express 같은 가벼운 서버
- 데이터베이스: Railway에서 제공하는 PostgreSQL
- 로그인: 간단한 비밀번호 인증 (사내용이라 복잡한 OAuth 불필요. 환경변수에 팀 공용 비번 하나 + 세션이면 충분)
- 배포: GitHub에 push하면 Railway가 자동 재배포

### 지금 Firebase가 하던 일 (이걸 대체해야 함)
1. **로그인**: Firebase Auth로 로그인 → 비밀번호 기반 간단 인증으로 대체
2. **데이터 저장 (Firestore)**: 아래 2개를 PostgreSQL 테이블로 옮겨야 함
   - `dashboard/soonNotes` : 출시예정 코멘트·전략 (텍스트 2개: comment, strategy)
   - `dashboard/teamData` : 팀원 관리 주차별 데이터 (JSON 통째로)
3. 그 외 판매 데이터(`const BASE`), 목표(`MONTH_GOALS`, `PROD_GOALS`), 출시예정(`SOON_ITEMS`)은
   지금 HTML 안에 JSON으로 박혀 있음. 이건 일단 그대로 둬도 되고, 나중에 DB로 옮겨도 됨.
   → 우선 1단계에서는 **저장이 필요한 soonNotes와 teamData만** DB로 옮기는 데 집중해줘.

### 진행 순서 (이대로 해줘)
1. 프로젝트 구조 잡기: `package.json`, `server.js`(Express), `public/dashboard.html`로 정리
2. Express 서버: 정적 파일(dashboard.html) 서빙 + API 엔드포인트
   - `GET /api/notes`, `POST /api/notes` (soonNotes)
   - `GET /api/team`, `POST /api/team` (teamData)
   - 간단 로그인: `POST /api/login` (환경변수 비번 확인 → 세션), 로그인 안 하면 차단
3. dashboard.html 수정: `db.collection(...)` 부분(2군데)을 위 API 호출(fetch)로 교체
   - `saveSoonNotes()` → `POST /api/notes`
   - `saveTeamData()` → `POST /api/team`
   - 페이지 로드 시 `GET`으로 불러와 화면에 반영
   - Firebase SDK 로드/초기화/auth 코드는 전부 제거
4. 로컬 테스트: `npm install` → `npm start` → localhost에서 동작 확인
5. GitHub: 저장소 만들고 push (git 명령 알려줘. .gitignore에 node_modules, .env 넣기)
6. Railway: GitHub 연결 → PostgreSQL 추가 → 환경변수(DB접속, 로그인비번, 세션시크릿) 설정 → 배포
7. 배포 후 URL 접속 테스트, 로그인·저장 동작 확인

### 중요한 계산 규칙 (절대 바꾸지 말 것 — 화면 로직 건드릴 때 주의)
- 상품원가 = N열 × 수량 = AO열 (AO만 사용, 더하지 말 것)
- 판매가는 이미 VAT 포함 → ×1.1 금지
- 상품이익률 = (판매가 − AO) ÷ 판매가 × 100
- 원가율 = AO ÷ 판매가 × 100
- 체험단 제외: 코드1 = 팬슈머마케팅팀/국내사업팀/브랜드디자인팀
- 이 로직은 이미 dashboard.html 안에 다 구현돼 있으니 건드리지 말고, 저장/로그인 부분만 바꿔줘.

### 내 환경
- VS Code 설치됨, Node.js 설치됨(아니면 설치부터 알려줘)
- GitHub 계정 있음(없으면 만드는 법 알려줘)
- Railway 계정 만들 예정

우선 1단계(프로젝트 구조 잡기)부터 시작하고, 매 단계 끝에서 내가 확인할 수 있게 멈춰줘.

## (여기까지 복사)

---

## 막혔을 때 저(claude.ai)에게 물어볼 것
- "Railway에서 PostgreSQL 환경변수를 어떻게 연결해?"
- "git push가 안 돼" / "Railway 배포가 실패했어" (에러 메시지 캡처해서)
- "로그인 후에도 화면이 안 보여"
- 클로드 코드가 시킨 명령의 뜻이 궁금할 때
