# Railway 이전 — 큰 그림 안내서 (지니님용)

클로드 코드가 작업하는 동안, 지금 무슨 일이 일어나는지 이해하기 위한 지도예요.

## 한 장 요약

```
[ 내 컴퓨터(VS Code) ]  --- git push --->  [ GitHub(코드 창고) ]  --- 자동 --->  [ Railway(웹 서버) ]
        ↑ 수정                                                                          ↓ 인터넷 주소
    클로드 코드가 작업                                                          팀이 접속해서 봄
```

- **GitHub** = 코드 보관 창고. 수정할 때마다 여기 올림(push). 기록·되돌리기 가능.
- **Railway** = 그 코드를 실제로 인터넷에서 돌려주는 서버. GitHub에 올리면 자동으로 웹사이트 갱신.
- **PostgreSQL** = Railway 안에 두는 데이터 저장소. (Firebase의 Firestore 역할)

## 앞으로 "업데이트"는 이렇게

수정하고 싶을 때:
1. (VS Code에서) 클로드 코드한테 수정 요청
2. 클로드 코드가 고친 뒤 `git push` (이 명령은 클로드 코드가 알려줌)
3. Railway가 자동으로 감지해서 웹사이트 갱신 (몇 분)
4. 새로고침하면 반영됨

→ 즉 한 번 세팅해두면, 이후엔 "수정 → push → 끝" 이에요.

## Firebase는 어떻게 되나

지금 쓰던 Firebase(로그인·저장)는 **이번에 걷어내요.** 그 기능을 Railway 위에서 새로 만들어요:
- 로그인 → 팀 공용 비밀번호 방식(간단)
- 데이터 저장 → PostgreSQL

작업이 끝나면 Firebase는 더 안 써도 돼요. (기존 Firebase에 저장됐던 코멘트·팀원 데이터가 있다면,
클로드 코드한테 "기존 Firestore 데이터를 옮겨줘"라고 부탁할 수도 있어요. 양이 적으면 그냥 다시 입력해도 됨.)

## 내가 직접 해야 하는 것 (클로드 코드가 다 알려줌)
1. GitHub 계정 만들기(없으면) + 저장소 1개 생성
2. Railway 계정 만들기(github로 로그인 가능)
3. 클로드 코드가 주는 터미널 명령 복사·붙여넣기
4. Railway 화면에서 버튼 몇 개 누르기(PostgreSQL 추가, 환경변수 입력)

코딩은 클로드 코드가 다 해요. 나는 명령 붙여넣고 버튼 누르는 정도.

## 비용
- GitHub: 무료
- Railway: 무료 크레딧 제공(소규모는 보통 월 $5 안쪽). 사용량 보고 결정 가능.
- PostgreSQL: Railway 안에서 무료 크레딧으로 커버되는 수준

## 막히면
claude.ai 채팅(지금 이 대화)으로 와서, 막힌 단계와 에러 화면을 캡처해서 물어보세요.
"클로드 코드가 이렇게 하라는데 무슨 뜻이야?"도 환영이에요.

## 함께 받은 파일
- `클로드코드_지시문.md` : VS Code 클로드 코드에 **그대로 복사해 붙여넣을** 지시문 (핵심!)
- `dashboard.html` : 지금까지 만든 완성 대시보드 (이걸 옮기는 거예요)
