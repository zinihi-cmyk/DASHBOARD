# 백엔드 적용 안내서 (admin 로그인 + DB)

받으신 `railway_backend` 폴더 파일들을 GitHub에 올리면, Railway가 백엔드로 띄워줍니다.
그러면 **admin으로 로그인한 사람만 매출 데이터를 볼 수 있게** 됩니다.

## 폴더 구조 (이대로 저장소에 올라가야 함)
```
저장소/
├─ server.js
├─ package.json
├─ .gitignore
└─ public/
   └─ index.html      ← 대시보드 (최신, firebase 제거됨)
```
※ 중요: 기존에 저장소 루트에 있던 index.html은 삭제하고, 위처럼 public/ 안에 넣으세요.

---

## 1단계 — 파일 올리기 (터미널)

저장소 폴더에서:
```
git rm index.html
```
받으신 파일(server.js, package.json, .gitignore, public/index.html)을 저장소 폴더에 복사한 뒤:
```
git add .
git commit -m "백엔드 로그인(admin) 추가, firebase 제거"
git push
```
push하면 Railway가 자동 빌드 (1~3분).

---

## 2단계 — admin 비밀번호 해시 만들기

비번은 그대로 넣지 않고 암호화(해시)해서 넣습니다.

1. Railway → DASHBOARD 서비스 → Variables 에 임시로 추가:
   - GEN_HASH_ENABLE = true
   (저장하면 자동 재배포됨, 1~2분 대기)
2. 브라우저에서 접속: https://(내주소)/gen-hash?pw=원하는비밀번호
   - 예: https://dashboard-production-c3e5.up.railway.app/gen-hash?pw=ggumbi2026
   - 화면에 해시($2a$10$...로 시작하는 긴 문자열)가 나옵니다. 복사하세요.

---

## 3단계 — Railway 환경변수 설정

DASHBOARD 서비스 → Variables 에 아래 4개를 설정:

| 변수 이름        | 값 |
|-----------------|----|
| USERS           | [{"id":"admin","pw":"여기에_2단계_해시_붙여넣기"}] |
| SESSION_SECRET  | 아무 긴 무작위 문자열 (예: ggumbi-secret-9f8a2c7e1b) |
| NODE_ENV        | production |
| DATABASE_URL    | Postgres 연결 (아래 참고) |

USERS 예시 (해시는 2단계에서 만든 본인 것으로 교체):
```
[{"id":"admin","pw":"$2a$10$9lya.tRk2WccB1CUmF2K7Ovez.vysimtZkweU/5bAlEDAMaRkfzy."}]
```
- 대괄호 [ ], 중괄호 { }, 따옴표 " 모두 그대로 있어야 합니다 (JSON 형식).
- 나중에 사람을 추가하려면 ,{"id":"다른아이디","pw":"다른해시"} 를 대괄호 안에 이어 붙이면 됩니다.

DATABASE_URL 연결:
- Variables에서 새 변수 추가 시 Railway가 ${{Postgres.DATABASE_URL}} 같은 참조를 제안합니다.
  그걸 선택하면 PostgreSQL과 자동 연결됩니다.
- 헷갈리면 Variables 화면을 캡처해서 claude.ai 채팅에 물어보세요.

4. 계정 설정이 끝나면 보안을 위해 GEN_HASH_ENABLE 을 false 로 바꾸세요.

---

## 4단계 — 확인

1. 내 주소 접속 → 로그인 화면만 보이고 매출 데이터는 안 보임 (정상)
2. 아이디 admin + 정한 비번으로 로그인 → 대시보드 보임
3. 로그아웃 → 다시 가려짐
4. 출시예정 코멘트/팀원 데이터 입력·저장 → 새로고침해도 유지되면 DB 저장 성공

---

## 작동 원리
- 매출·코멘트·팀원 데이터는 전부 /api/... 로만 오갑니다.
- 그 API는 로그인 안 하면 401로 막습니다 → 주소를 알아도 로그인 못 하면 서버가 데이터를 아예 안 줍니다.

## 막히면 (claude.ai 채팅에 캡처해서)
- "git push 에러" / "Railway 빌드 실패(Deploy 로그)"
- "DATABASE_URL 연결을 어떻게 해?"
- "gen-hash 페이지가 안 떠" → GEN_HASH_ENABLE=true 됐는지, 재배포 끝났는지 확인
- "로그인은 되는데 새로고침하면 풀려" → NODE_ENV=production 확인
- "USERS 넣었는데 로그인 실패" → JSON 따옴표/대괄호 형식 확인 (제일 흔한 실수)

## 다음 단계 (나중에)
지금은 코멘트·팀원 데이터만 DB 저장됩니다. 판매데이터까지 DB로 옮기는 건 이게 안정화된 뒤 진행하세요.
